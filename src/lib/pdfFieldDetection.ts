/**
 * Compatibility re-export for the raster heuristics detector.
 * The runtime detector logic now lives in src/lib/detectors/rasterHeuristics.
 */

export { PDFFieldDetector, type DetectionOptions } from "./detectors/rasterHeuristics";

